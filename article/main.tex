\documentclass[11pt,a4paper]{article}

% --- Packages ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{xcolor}
\usepackage{float}
\usepackage[margin=2.5cm]{geometry}
\usepackage{authblk}
\usepackage{listings}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{subcaption}
\usepackage{multirow}

\hypersetup{colorlinks=true,linkcolor=blue!60!black,citecolor=green!50!black,urlcolor=blue!60!black}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  breaklines=true,
  frame=single,
  language=Python,
}

% --- Title ---
\title{Activation Steering for Small Language Models:\\
From Mid-Layer Sweet Spots to Dynamic Multi-Agent Orchestration}

\author[1]{Arthur Edmond}
\affil[1]{LLM Engineer, Swapn}

\date{March 2026}

\begin{document}
\maketitle

% ===================================================================
\begin{abstract}
We present a comprehensive study of \textbf{activation steering} for improving task-specific behavior in small language models (SLMs), from mechanistic analysis to practical multi-agent orchestration. Using Qwen3-4B and Qwen3-0.6B as primary models, and extending to Llama-3.2-3B-Instruct and LFM2.5-1.2B-Instruct for cross-architecture analysis, we (1)~analyze tokenizer fragmentation across four models, (2)~extract contrastive steering vectors via SAE-inspired methodology, (3)~perform a layer$\times$coefficient sweep identifying mid-layer sweet spots, (4)~validate on base (non-instruct) models, (5)~evaluate on GSM8K math reasoning, (6)~quantify effects via KL divergence and sampling diversity, (7)~build a dynamic multi-agent steering orchestrator, (8)~test on SWE-bench Verified with RAG-augmented patch generation, (9)~demonstrate that domain steering geometry is architecture-invariant across three fundamentally different architectures, (10)~benchmark on MMLU-Pro in both generation and loglikelihood modes with rigorous sample sizing ($n=200$), revealing that steering degrades both modes---catastrophically for generation, modestly for scoring---and that small-sample evaluations ($n=20$) produce misleading false positives, (11)~decompose contrastive steering vectors via a custom-trained SAE, revealing near-zero overlap with domain-specific features and explaining why contrastive vectors capture ``style'' but not ``knowledge,'' and (12)~replicate the full SAE pipeline on Qwen3-4B, demonstrating that steering's inability to inject knowledge is scale-independent despite improved feature alignment.

Our key findings are: (a)~\textbf{mid-layer steering (layers 15--18) achieves 100\% behavioral change} at moderate coefficients ($\alpha=30$), while late layers (33--35) are rigid---an \textit{architectural} property independent of instruction tuning; (b)~\textbf{base models are more steerable but fragile}---effective at $\alpha=30$ but degenerate at $\alpha\geq60$; (c)~\textbf{steering on a 0.6B SLM boosts GSM8K accuracy by +16pp} under zero-shot CoT (46\%$\to$62\%), but \textbf{degrades under 5-shot} ($-$8pp), revealing destructive interference between steering and few-shot prompting; (d)~\textbf{KL divergence confirms a 3-order-of-magnitude gap} between mid-layer (1--48 bits) and late-layer ($<$0.01 bits) steering; (e)~a \textbf{dynamic steering orchestrator} that switches domain-specific vectors per plan step achieves 4--7$\times$ more domain-relevant outputs than baseline; (f)~on SWE-bench Verified, RAG-augmented steering produces patches with \textbf{68\% valid file paths} (vs.\ 0\% without RAG), with 95\% of instances generating syntactically valid diffs; (g)~\textbf{domain steering geometry is architecture-invariant} (Spearman $\rho > 0.88$ across Transformer, larger Transformer, and hybrid SSM+Attention models), with history as a universal outlier---though direct vector transfer remains impossible due to differing hidden dimensions. We present a validated architecture for training-free enhancement of SLM agents via sequential steering with adaptive coefficients.
\end{abstract}

\textbf{Keywords:} activation steering, small language models, mechanistic interpretability, sparse autoencoders, multi-agent orchestration, SWE-bench, dynamic steering, cross-architecture transfer, MMLU-Pro, model scaling

% ===================================================================
\section{Introduction}
\label{sec:intro}

Small Language Models (SLMs) with 0.6--3B parameters are increasingly deployed as agents that must extract structured information and execute tool calls. However, their limited capacity makes them sensitive to how tasks are framed at inference time. Two families of techniques compete for improving their task-specific behavior:

% removed redundant intro text

\begin{enumerate}
    \item \textbf{Prompt engineering} --- crafting system prompts, few-shot examples, and chain-of-thought instructions to guide generation at inference time.
    \item \textbf{Activation steering} --- adding learned or computed vectors to the model's residual stream during forward passes, modifying internal representations without changing weights~\cite{turner2023activation,li2024inference}.
\end{enumerate}

Recent work has shown that per-layer steering vectors trained via reinforcement learning can match fully fine-tuned models on mathematical reasoning tasks, adding only 0.0016\% parameters~\cite{gao2025steering}. Separately, budget guidance techniques demonstrate training-free control of reasoning length via token-level distribution modeling~\cite{li2025budget}. However, these approaches have primarily been studied on reasoning benchmarks --- their effectiveness in agentic tool-use contexts remains unexplored.

\textbf{Contributions.} This paper makes eleven contributions:
\begin{itemize}
    \item A comparative tokenizer analysis across four models revealing universal fragmentation of structured expressions (\cref{sec:tokenizers}).
    \item SAE-inspired contrastive feature extraction with logit lens validation (\cref{sec:features}).
    \item A comprehensive steering sweep across 11 layers and 6 coefficients, identifying mid-layer steering (layers 15--18) as the effective intervention point (\cref{sec:steering}).
    \item Comparative steering on the base (non-instruct) model, revealing higher malleability but greater fragility (\cref{sec:base_steering}).
    \item Zero-shot steering synergy on GSM8K ($+$16pp) contrasted with destructive 5-shot interference (\cref{sec:gsm8k}).
    \item A dynamic multi-agent steering orchestrator with domain-specific vector switching (\cref{sec:dynamic_steering}).
    \item RAG-augmented SWE-bench evaluation showing 0\%$\to$68\% path validity via steering + retrieval (\cref{sec:swebench}).
    \item Cross-architecture analysis showing domain geometry is invariant across Transformer and hybrid SSM+Attention models ($\rho > 0.88$), with Procrustes-aligned MDS revealing a universal domain map (\cref{sec:cross_architecture}).
    \item MMLU-Pro dual-mode evaluation (generation vs.\ loglikelihood) across 3 models $\times$ 3 domains, with $n=200$ validation revealing that both modes degrade under steering and that small-sample ($n=20$) results produce false positives (\cref{sec:mmlu_pro}).
    \item SAE feature decomposition showing that contrastive steering vectors activate 50--60\% of SAE features diffusely, with near-zero overlap (0--3/20) with domain-specific features, explaining why they capture ``style'' but not ``knowledge'' (\cref{sec:sae_analysis}).
    \item A scaling comparison on Qwen3-4B replicating the full SAE pipeline, showing that despite higher baselines (math 48\% vs 18\%, history 34\% vs 14\%), less diffuse contrastive vectors (41--51\% vs 47--59\%), and improved contrastive-SAE overlap (5/20 vs 0/20 for law), \textbf{no steering method exceeds baseline accuracy}---confirming that steering captures style, not knowledge, independent of model scale (\cref{sec:sae_analysis}).
    \item A cross-architecture validation on LFM2-700M (hybrid conv+attention), requiring a custom SAE training pipeline via HuggingFace hooks. Despite the least diffuse contrastive vectors (26--38\%), the style-not-knowledge pattern is confirmed as \textbf{architecture-invariant} (\cref{sec:lfm2}).
\end{itemize}

% ===================================================================
\section{Related Work}
\label{sec:related}

\subsection{Activation Steering}
Turner et al.~\cite{turner2023activation} introduced activation addition, showing that adding contrastive mean-difference vectors to residual streams can control model behavior (e.g., shifting sentiment or honesty). Gao et al.~\cite{gao2025steering} extended this to reinforcement learning, training per-layer bias vectors that match full RL fine-tuning on math reasoning with extreme parameter efficiency. Li et al.~\cite{li2025budget} proposed budget guidance, a training-free method using Gamma-distribution predictors to control reasoning length at inference time.

\subsection{Mechanistic Interpretability}
Sparse Autoencoders (SAEs) decompose model activations into interpretable features~\cite{cunningham2023sparse}. The Gemma Scope project~\cite{lieberum2024gemma} provides SAEs, transcoders, and crosscoders trained on every layer of Gemma models. Neuronpedia\footnote{\url{https://www.neuronpedia.org}} offers circuit-level attribution graphs for models including Qwen3-4B.

\subsection{Prompt Engineering for Tool Use}
Function calling and structured output extraction have been studied extensively in the context of LLM agents~\cite{schick2023toolformer}. Chain-of-thought prompting~\cite{wei2022chain} and few-shot examples remain strong baselines for structured extraction tasks.

% ===================================================================
\section{Tokenizer Analysis}
\label{sec:tokenizers}

Tokenization quality directly impacts downstream model behavior: a tokenizer that fragments semantic units distributes information across multiple positions in the residual stream, potentially diluting features that steering vectors target.

\subsection{Experimental Setup}

We compare four tokenizers on nine prompt categories designed for agentic calendar tasks:

\begin{table}[H]
\centering
\caption{Tokenizer properties.}
\label{tab:tokenizers}
\begin{tabular}{lcc}
\toprule
\textbf{Model} & \textbf{Vocab Size} & \textbf{Architecture} \\
\midrule
Qwen3-4B-Instruct-2507 & 151,643 & BPE \\
Gemma-3-1B-IT & 262,144 & SentencePiece \\
Llama-3.2-3B-Instruct & 128,000 & BPE \\
Phi-3-mini-4k-instruct & 32,000 & SentencePiece \\
\bottomrule
\end{tabular}
\end{table}

Our test suite includes simple and complex calendar requests in French and English, relative and absolute temporal expressions, JSON tool calls, and system prompts.

\subsection{Results}

\subsubsection{Token Counts and Compression}

\begin{table}[H]
\centering
\caption{Token counts across prompt categories. Lower is better.}
\label{tab:token_counts}
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Prompt} & \textbf{Qwen3} & \textbf{Gemma-3} & \textbf{Llama-3.2} & \textbf{Phi-3} \\
\midrule
simple\_fr & 20 & 19 & 19 & 22 \\
simple\_en & 15 & 15 & 15 & 17 \\
complex\_fr & 55 & 50 & 51 & 62 \\
complex\_en & 47 & 45 & 43 & 53 \\
ambiguous & 18 & 16 & 18 & 22 \\
temporal\_relative & 23 & 20 & 24 & 27 \\
temporal\_absolute & 43 & 43 & 29 & 44 \\
tool\_call\_json & 125 & 125 & 106 & 136 \\
system\_agent & 34 & 36 & 34 & 41 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Llama-3.2-3B achieves the best compression} on both temporal expressions (29 tokens vs.\ 43--44 for ISO 8601 dates) and JSON tool calls (106 vs.\ 125--136). This is attributable to its BPE vocabulary including multi-digit numbers (\texttt{``24''}, \texttt{``202''}) and code-oriented subwords (\texttt{``\_calendar\_event''} as a single token).

\subsubsection{Quality Scoring}

We designed a composite scoring function evaluating four dimensions relevant to agentic calendar use:

\begin{table}[H]
\centering
\caption{Tokenizer quality scores for calendar tasks (0--1 scale, higher is better).}
\label{tab:quality_scores}
\begin{tabular}{lccccc}
\toprule
\textbf{Model} & \textbf{Temporal} & \textbf{Semantic} & \textbf{JSON} & \textbf{Multilingual} & \textbf{Overall} \\
\midrule
Qwen3-4B & 0.205 & 0.680 & 0.288 & 0.802 & 0.483 \\
Gemma-3-1B & 0.226 & 0.722 & 0.288 & 0.845 & 0.511 \\
Llama-3.2-3B & 0.316 & 0.726 & 0.375 & 0.816 & \textbf{0.551} \\
Phi-3-mini & 0.173 & 0.580 & 0.249 & 0.814 & 0.438 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key finding:} All tokenizers score poorly on temporal integrity ($<0.32$). ISO 8601 timestamps are universally fragmented into individual characters, creating a distributed representation that complicates feature extraction for steering.

% ===================================================================
\section{Feature Extraction via Contrastive Activations}
\label{sec:features}

\subsection{Methodology}

Inspired by the Gemma Scope 2 pipeline~\cite{lieberum2024gemma} and contrastive activation addition~\cite{turner2023activation}, we extract steering-relevant features without training SAEs. Our approach:

\begin{enumerate}
    \item Collect last-token residual stream activations at all 36 layers for 10 calendar and 10 neutral prompts.
    \item Compute per-layer mean activations for each group.
    \item Define the \textbf{steering vector} as the mean difference: $\mathbf{v}_l = \bar{\mathbf{a}}_l^{\text{cal}} - \bar{\mathbf{a}}_l^{\text{neu}}$.
    \item Rank layers by $\|\mathbf{v}_l\|_2$ (separation magnitude) and cosine distance.
\end{enumerate}

\subsection{Layer Importance}

\begin{table}[H]
\centering
\caption{Top-10 layers by steering vector L2 norm.}
\label{tab:layer_importance}
\begin{tabular}{cccc}
\toprule
\textbf{Rank} & \textbf{Layer} & \textbf{L2 Norm} & \textbf{Cosine Distance} \\
\midrule
1 & 35 & 361.00 & 0.151 \\
2 & 34 & 218.63 & 0.075 \\
3 & 33 & 179.38 & 0.094 \\
4 & 32 & 162.50 & 0.104 \\
5 & 31 & 143.63 & 0.113 \\
6 & 30 & 126.19 & 0.120 \\
7 & 29 & 110.31 & 0.130 \\
8 & 28 & 100.44 & 0.151 \\
9 & 27 & 89.50 & 0.164 \\
10 & 26 & 79.50 & 0.168 \\
\bottomrule
\end{tabular}
\end{table}

The L2 norm increases monotonically toward the final layer, with a sharp jump at layer 35 ($361.0$ vs.\ $218.6$ at layer 34). This is consistent with the ``representation engineering'' hypothesis: later layers encode increasingly task-specific, high-level semantic features.

\subsection{Logit Lens Analysis}

We project the top-3 steering vectors through the unembedding matrix ($\mathbf{W}_U$) after applying RMSNorm weights:

$$\text{logits}_l = \mathbf{W}_U \cdot (\mathbf{w}_{\text{norm}} \odot \mathbf{v}_l)$$

\begin{table}[H]
\centering
\caption{Top tokens promoted/suppressed by steering vectors (layers 33--35).}
\label{tab:logit_lens}
\small
\begin{tabular}{cl}
\toprule
\textbf{Layer} & \textbf{Top Promoted Tokens} \\
\midrule
35 & \texttt{Schedule}, \texttt{Agenda}, \texttt{agenda}, \texttt{schedule}, \texttt{Scheduled} \\
34 & \texttt{schedule}, \texttt{agenda}, \texttt{attendees}, \texttt{RSVP}, \texttt{calendar} \\
33 & \texttt{agenda}, \texttt{schedule}, \texttt{attendees}, \texttt{invite}, \texttt{scheduled} \\
\midrule
\textbf{Layer} & \textbf{Top Suppressed Tokens} \\
\midrule
35 & \texttt{Magnitude}, \texttt{hesion}, \texttt{magnitude}, \texttt{density}, \texttt{humidity} \\
34 & \texttt{Magnitude}, \texttt{licking}, \texttt{entropy}, \texttt{Measured} \\
33 & \texttt{entropy}, \texttt{Measured}, \texttt{Magnitude} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Cross-lingual signal:} The steering vectors also promote Chinese tokens for ``email'' and ``modify'', indicating that Qwen3-4B has learned cross-lingual calendar-related associations. This is notable given the model's multilingual training on 119 languages.

% ===================================================================
\section{Steering Vector Application}
\label{sec:steering}

\subsection{Methodology}

We apply steering vectors using activation addition~\cite{turner2023activation}:

$$\mathbf{h}_l' = \mathbf{h}_l + \alpha \cdot \frac{\mathbf{v}_l}{\|\mathbf{v}_l\|}$$

where $\alpha \in \{0, 10, 30, 60, 100, 200\}$ and $\mathbf{h}_l$ is the residual stream at layer $l$. We sweep across 11 layers ($l \in \{5, 10, 15, 18, 20, 22, 25, 28, 30, 33, 35\}$) on six prompts: 2 calendar (FR/EN), 2 ambiguous (FR/EN), and 2 non-calendar (FR/EN).

\subsection{Initial Finding: Late-Layer Rigidity}

Applying the layer-35 vector (highest L2 norm, 361.0) produces \textbf{zero behavioral change} across all coefficients up to $\alpha=50$. All responses are character-for-character identical to baseline. This motivates the broader layer sweep.

\subsection{Mid-Layer Steering Sweet Spot}

\begin{table}[H]
\centering
\caption{Change rate (\%) vs.\ baseline across layers and coefficients on Qwen3-4B-Instruct. Bold indicates 100\% change rate with coherent outputs.}
\label{tab:midlayer_sweep}
\small
\begin{tabular}{rcccccc}
\toprule
& \multicolumn{6}{c}{\textbf{Steering Coefficient ($\alpha$)}} \\
\cmidrule(lr){2-7}
\textbf{Layer} & 0 & 10 & 30 & 60 & 100 & 200 \\
\midrule
5  & 0 & 33 & 33 & 100$^\dagger$ & 67$^\dagger$ & 67$^\dagger$ \\
10 & 0 & 17 & 67 & 83 & 67 & 100 \\
\textbf{15} & \textbf{0} & \textbf{17} & \textbf{100} & \textbf{100} & \textbf{83} & \textbf{100} \\
\textbf{18} & \textbf{0} & \textbf{33} & \textbf{100} & \textbf{100} & \textbf{100} & \textbf{67} \\
20 & 0 & 17 & 33 & 100 & 100 & 67 \\
22 & 0 & 17 & 17 & 50 & 100 & 67 \\
25 & 0 & 0 & 17 & 33 & 50 & 83 \\
30 & 0 & 0 & 17 & 17 & 17 & 50 \\
33 & 0 & 0 & 17 & 17 & 17 & 17 \\
35 & 0 & 0 & 0 & 0 & 0 & 17 \\
\bottomrule
\end{tabular}
\vspace{2pt}

{\footnotesize $^\dagger$ Layer 5 at high $\alpha$ produces degenerate (empty) outputs.}
\end{table}

\textbf{Key result:} Layers 15--18 at $\alpha=30$ achieve 100\% change rate with coherent, well-formed responses. This is the \textit{steering sweet spot}---representations at these layers are transitioning from syntactic to semantic and remain malleable enough for intervention.

\textbf{Qualitative effect:} Non-calendar prompts get reinterpreted as calendar tasks. For instance, \textit{``Explain how transformers work''} becomes \textit{``Let's schedule a meeting to discuss transformers''}---the steering vector genuinely redirects the model's intent, not just its surface tokens.

A clear \textbf{gradient of rigidity} emerges: late layers (30--35) require 3--10$\times$ higher coefficients for the same effect, and even then produce only partial changes. This confirms the hypothesis that instruction tuning ``freezes'' late-layer representations.

% ===================================================================
\section{Base Model Steering}
\label{sec:base_steering}

\subsection{Setup}

To disentangle the effect of instruction tuning, we repeat the steering experiment on \textbf{Qwen3-4B} (base, non-instruct). We extract fresh contrastive vectors using completion-style prompts (no chat template) with 10 calendar and 10 neutral examples.

\subsection{Results}

\begin{table}[H]
\centering
\caption{Calendar score (0--1) for base model steering. Higher = stronger calendar signal. Baseline avg = 0.70.}
\label{tab:base_steering}
\small
\begin{tabular}{rcccccc}
\toprule
& \multicolumn{6}{c}{\textbf{Steering Coefficient ($\alpha$)}} \\
\cmidrule(lr){2-7}
\textbf{Layer} & 0 & 10 & 30 & 60 & 100 & 200 \\
\midrule
10 & 0.70 & 0.73 & 0.70 & 0.73 & 0.23 & 0.00 \\
\textbf{15} & \textbf{0.70} & \textbf{0.70} & \textbf{0.97} & \textbf{0.33} & \textbf{0.03} & \textbf{0.00} \\
18 & 0.70 & 0.73 & 0.73 & 0.13 & 0.00 & 0.00 \\
20 & 0.70 & 0.73 & 0.73 & 0.03 & 0.00 & 0.00 \\
25 & 0.70 & 0.73 & 0.73 & 0.73 & 0.77 & 0.00 \\
30 & 0.70 & 0.70 & 0.70 & 0.73 & 0.73 & 0.70 \\
35 & 0.70 & 0.70 & 0.70 & 0.70 & 0.73 & 0.77 \\
\bottomrule
\end{tabular}
\end{table}

Three findings emerge:

\begin{enumerate}
    \item \textbf{Layer 15 at $\alpha=30$ is the optimal point}: calendar score jumps from 0.70 to 0.97 ($+0.27$), matching the instruct model's sweet spot layer.
    \item \textbf{Base models are much more fragile}: at $\alpha \geq 60$, scores collapse to near-zero (degenerate output). The instruct model tolerates $\alpha=200$ at mid-layers without complete degeneration.
    \item \textbf{Late-layer rigidity is architectural}: layers 30--35 resist steering on both instruct and base models, ruling out instruction tuning as the sole cause. This is a property of the transformer architecture itself---late residual streams are too committed to the output distribution.
\end{enumerate}

% ===================================================================
\section{Budget Guidance}
\label{sec:budget}

Inspired by Li et al.~\cite{li2025budget}, we implement a simplified budget guidance mechanism: a Gamma-distribution predictor that biases the EOS token logit as generation approaches a token budget, using a sigmoid urgency ramp.

\subsection{Results}

We test budgets of 32, 64, 128, 256, and 512 tokens on 5 calendar prompts:

\begin{table}[H]
\centering
\caption{Budget guidance results on Qwen3-4B-Instruct. Baseline avg: 75 tokens.}
\label{tab:budget}
\begin{tabular}{rcccc}
\toprule
\textbf{Budget} & \textbf{Avg Tokens} & \textbf{Valid} & \textbf{Within Budget} & \textbf{Savings} \\
\midrule
$\infty$ (baseline) & 74.6 & 100\% & 100\% & 0\% \\
32 & 74.6 & 100\% & 0\% & 0\% \\
64 & 74.6 & 100\% & 0\% & 0\% \\
128 & 74.6 & 100\% & 100\% & 0\% \\
256 & 74.6 & 100\% & 100\% & 0\% \\
512 & 74.6 & 100\% & 100\% & 0\% \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Budget guidance has zero effect.} The instruct model already generates near-minimal outputs ($\sim$75 tokens of compact JSON). Even the most aggressive budget (32 tokens) fails to further compress the output---the model's greedy decoding path is already optimal for structured extraction. This contrasts with reasoning-heavy tasks (e.g., GSM8K) where models produce verbose chain-of-thought before answering, and where budget guidance can yield significant savings~\cite{li2025budget}.

\textbf{Implication:} Budget guidance targets \textit{thinking overhead}, not output verbosity. For tasks where instruction tuning has already eliminated unnecessary reasoning tokens, it adds no value.

% ===================================================================
\section{Neuronpedia Feature Mapping}
\label{sec:neuronpedia}

We explore Qwen3-4B's SAE features via Neuronpedia's transcoder dictionary (163,840 features per layer) using three strategies: explanation keyword search, random sampling, and targeted key-layer scans.

\subsection{Results}

\begin{itemize}
    \item \textbf{Explanation search}: 115 unique features matched calendar keywords (schedule, meeting, date, time, etc.) across layers 20--35.
    \item \textbf{Random sampling}: only 9 out of 450 sampled features (2\%) were calendar-related, confirming extreme sparsity.
    \item \textbf{Feature distribution}: calendar features concentrate in later layers (20--35), consistent with our contrastive analysis. The highest-layer features show the most specific explanations (e.g., ``scheduling appointments'' vs.\ generic ``time references'').
\end{itemize}

The 0.07\% density of calendar features (9/450 random samples $\times$ 163,840 features/layer) implies approximately 115 calendar-relevant features per layer---a tiny fraction of the total, but sufficient to encode the task-specific behavior observed in our steering experiments.

% ===================================================================
\section{SLM Steering on GSM8K}
\label{sec:gsm8k}

To test whether steering generalizes beyond calendar tasks and instruction-tuned models, we run a parallel experiment on \textbf{Qwen3-0.6B} (28 layers, 1024 hidden dim) using math reasoning (GSM8K) as the target task.

\subsection{Setup}
We extract contrastive vectors from 10 ``math reasoning'' prompts vs.\ 10 neutral prompts. We evaluate on 10 GSM8K problems using three prompt strategies (zero-shot, CoT, few-shot) and steering across 6 layers $\times$ 5 coefficients, on both the instruct and base variants.

\subsection{Results}

\begin{table}[H]
\centering
\caption{GSM8K accuracy (\%) on Qwen3-0.6B. Best steering result per layer in bold.}
\label{tab:gsm8k}
\small
\begin{tabular}{rcccccc}
\toprule
& \multicolumn{5}{c}{\textbf{Steering Coefficient ($\alpha$)}} \\
\cmidrule(lr){2-6}
\textbf{Layer} & 0 & 10 & 30 & 60 & 100 \\
\midrule
\multicolumn{6}{c}{\textit{Instruct model (baseline: 20\% zero-shot)}} \\
\midrule
5  & 20 & 0 & 0 & 0 & 0 \\
15 & 20 & 0 & 0 & 0 & 0 \\
18 & 20 & 10 & 20 & 10 & 0 \\
25 & 20 & 20 & 10 & \textbf{30} & 10 \\
\midrule
\multicolumn{6}{c}{\textit{Base model (baseline: 20\% few-shot)}} \\
\midrule
5  & 20 & 0 & 0 & 0 & 0 \\
15 & 20 & 20 & 0 & 10 & 0 \\
18 & 20 & 20 & \textbf{30} & 20 & 0 \\
20 & 20 & 20 & 20 & 30 & \textbf{40} \\
25 & 20 & 20 & 30 & 30 & 30 \\
\bottomrule
\end{tabular}
\end{table}

Three findings emerge:

\begin{enumerate}
    \item \textbf{Steering is more effective on the base model}: Layer 20 at $\alpha=100$ \textbf{doubles} GSM8K accuracy (20\%$\to$40\%). The instruct model only achieves $+10\%$ at one configuration (Layer 25, $\alpha=60$).
    \item \textbf{The sweet spot shifts with model depth}: On the 0.6B (28 layers), optimal steering occurs at layers 18--25 (64--89\% depth), compared to layers 15--18 (42--50\%) on the 4B (36 layers). This suggests a \textbf{relative depth hypothesis}: the steering sweet spot follows the syntactic$\to$semantic transition, which scales with total layer count.
    \item \textbf{SLMs are extremely fragile}: Early layers (5--10) at any non-zero coefficient universally destroy accuracy, and CoT prompting actually \textit{hurts} the 0.6B model (10\% vs.\ 20\% zero-shot).
\end{enumerate}

\subsection{Validation via lm-eval (n=50)}

To validate our pilot results with a standardized evaluation, we run the EleutherAI \texttt{lm-eval} harness~\cite{eval-harness} on two GSM8K task variants: the standard \textbf{5-shot} task and the \textbf{zero-shot CoT} task (\texttt{gsm8k\_cot\_zeroshot}, prompt: ``Let's think step by step''). We subclass \texttt{HFLM} to inject steering hooks during both \texttt{\_model\_call} and \texttt{\_model\_generate}.

\begin{table}[H]
\centering
\caption{GSM8K lm-eval results (n=50). 5-shot vs.\ zero-shot CoT reveal opposite steering effects.}
\label{tab:gsm8k_lmeval}
\small
\begin{tabular}{llcccc}
\toprule
& & \multicolumn{2}{c}{\textbf{5-shot}} & \multicolumn{2}{c}{\textbf{Zero-shot CoT}} \\
\cmidrule(lr){3-4} \cmidrule(lr){5-6}
\textbf{Model} & \textbf{Condition} & \textbf{Strict} & \textbf{Flexible} & \textbf{Strict} & \textbf{Flexible} \\
\midrule
Instruct & Baseline & 48\% & 48\% & 38\% & 46\% \\
Instruct & Steered & 44\% {\scriptsize($-$4)} & 40\% {\scriptsize($-$8)} & 40\% {\scriptsize(+2)} & \textbf{62\%} {\scriptsize(\textbf{+16})} \\
\midrule
Base & Baseline & 48\% & 48\% & 36\% & 28\% \\
Base & Steered & 26\% {\scriptsize($-$22)} & 34\% {\scriptsize($-$14)} & 8\% {\scriptsize($-$28)} & 22\% {\scriptsize($-$6)} \\
\bottomrule
\end{tabular}
\vspace{2pt}

{\footnotesize Instruct steered: L25@$\alpha$=60. Base steered: L20@$\alpha$=100.}
\end{table}

The 5-shot and zero-shot results tell \textbf{opposite stories}, revealing a critical interaction between steering and prompting strategy:

\begin{enumerate}
    \item \textbf{Zero-shot CoT + steering = synergy on instruct.} The steered instruct model achieves \textbf{62\% flexible-extract} ($+16$pp over baseline), the best result across all conditions. The strict match remains stable (38\%$\to$40\%), indicating that steering improves \textit{reasoning} without improving \textit{format compliance}---the model finds correct answers more often but doesn't always use the ``The answer is X'' template.
    \item \textbf{5-shot + steering = destructive interference.} Under 5-shot evaluation, steering \textit{degrades} both models. The few-shot exemplars already provide strong reasoning scaffolding ($\sim$350 context tokens); the steering vector is redundant with, and interferes with, this guidance. The steered base model drops from 48\%$\to$26\% strict match.
    \item \textbf{Base model at $\alpha$=100 degenerates regardless.} The base model shows degradation in both evaluation protocols. This confirms $\alpha$=100 is too aggressive for the 0.6B base model (consistent with the narrow stability window observed in \cref{sec:base_steering}). Lower coefficients ($\alpha$=30--60) would likely yield improvements.
    \item \textbf{Generation pathology at high $\alpha$.} Steered base model generation took $3.3\times$ longer under 5-shot (66 vs.\ 20 minutes), suggesting the steering vector pushes the model into longer, erratic generation paths before hitting a stop token.
\end{enumerate}

\textbf{Implication for dynamic steering:} Steering coefficients must be \textit{adaptive} to the prompting context. A coefficient that helps under zero-shot conditions can hurt when few-shot exemplars provide directional guidance. This motivates context-aware selection: $\alpha = f(n_{\text{few-shot}}, \text{model\_type}, \text{task\_domain})$---a key design requirement for the dynamic multi-agent steering architecture discussed in \cref{sec:discussion}.

% ===================================================================
\section{Sampling-Based Steering Analysis}
\label{sec:sampling}

Previous experiments used greedy decoding ($T{=}0$), which collapses all probability mass onto the argmax token. To detect subtler steering effects, we analyze the full logit distribution via KL divergence and measure response diversity under sampling ($T>0$).

\subsection{KL Divergence: Quantifying Distribution Shift}

We compute KL divergence (in bits) between unsteered and steered first-token logit distributions across 6 prompts (2 calendar, 2 ambiguous, 2 non-calendar) for layers 15, 18, and 35 at three coefficient levels.

\begin{table}[H]
\centering
\caption{KL divergence (bits) between steered and unsteered logit distributions. Mid-layers show 3--5 orders of magnitude more divergence than late layers.}
\label{tab:kl_divergence}
\begin{tabular}{l|rrr|rrr|rrr}
\toprule
& \multicolumn{3}{c|}{\textbf{Layer 15}} & \multicolumn{3}{c|}{\textbf{Layer 18}} & \multicolumn{3}{c}{\textbf{Layer 35}} \\
\textbf{Prompt} & $\alpha{=}10$ & $\alpha{=}30$ & $\alpha{=}60$ & $\alpha{=}10$ & $\alpha{=}30$ & $\alpha{=}60$ & $\alpha{=}10$ & $\alpha{=}30$ & $\alpha{=}60$ \\
\midrule
calendar\_fr & 0.01 & 4.20 & 44.4 & 0.01 & 0.61 & 46.4 & 0.00 & 0.00 & 0.00 \\
calendar\_en & 0.04 & 1.51 & 48.4 & 0.04 & 2.10 & 45.2 & 0.00 & 0.00 & 0.00 \\
ambiguous\_en & 0.01 & 11.0 & 48.1 & 0.02 & 1.50 & 45.4 & 0.00 & 0.00 & 0.00 \\
ambiguous\_fr & 0.00 & 10.6 & 45.4 & 0.03 & 2.61 & 43.1 & 0.00 & 0.00 & 0.00 \\
non\_cal\_fr & 1.17 & 21.2 & 24.5 & 1.28 & 23.5 & 30.1 & 0.00 & 0.00 & 0.01 \\
non\_cal\_en & 0.09 & 20.5 & 33.2 & 0.02 & 17.8 & 32.7 & 0.00 & 0.00 & 0.00 \\
\bottomrule
\end{tabular}
\end{table}

Three key observations emerge:
\begin{enumerate}
    \item \textbf{Late-layer rigidity is distributional, not just behavioral.} Layer 35 achieves at most 0.014 bits of KL divergence even at $\alpha{=}60$---the entire output distribution is effectively unchanged. This rules out the hypothesis that late-layer steering shifts probabilities subtly without changing the greedy argmax.
    \item \textbf{Non-calendar prompts show the highest mid-layer KL.} At $\alpha{=}30$, non-calendar prompts exhibit 17--23 bits of divergence (vs.\ 0.6--11 bits for calendar prompts), indicating that steering maximally disrupts prompts that conflict with the steering direction.
    \item \textbf{The $\alpha{=}10 \to 30$ transition is non-linear.} KL divergence jumps by 2--3 orders of magnitude between $\alpha{=}10$ ($<$1 bit for most prompts) and $\alpha{=}30$ (1--23 bits), suggesting a phase transition in representation override.
\end{enumerate}

\subsection{Sampling Diversity}

Under sampling ($T \in \{0.3, 0.7, 1.0\}$, 5 samples per condition), we measure response diversity (unique responses / total) for layer 15 and 18 at $\alpha{=}30$:

\begin{table}[H]
\centering
\caption{Response diversity (\% unique out of 5 samples) under sampling. Steering increases diversity while preserving JSON output type.}
\label{tab:sampling_diversity}
\begin{tabular}{l|ccc|ccc}
\toprule
& \multicolumn{3}{c|}{\textbf{Baseline}} & \multicolumn{3}{c}{\textbf{L15 @ $\alpha{=}30$}} \\
\textbf{Prompt} & $T{=}0.3$ & $T{=}0.7$ & $T{=}1.0$ & $T{=}0.3$ & $T{=}0.7$ & $T{=}1.0$ \\
\midrule
calendar\_fr & 20\% & 40\% & 60\% & 100\% & 100\% & 100\% \\
calendar\_en & 20\% & 20\% & 40\% & 80\% & 80\% & 80\% \\
ambiguous\_en & 20\% & 40\% & 40\% & 80\% & 100\% & 100\% \\
ambiguous\_fr & 20\% & 20\% & 60\% & 80\% & 100\% & 100\% \\
\bottomrule
\end{tabular}
\end{table}

Crucially, all steered samples remain valid JSON calendar outputs---steering flattens the probability distribution across \textit{valid} responses rather than introducing degenerate modes. This suggests mid-layer steering broadens the response manifold without breaking task competence, an effect invisible under greedy decoding.

% ===================================================================
\section{Prompt Engineering Baselines}
\label{sec:baselines}

We define five prompting strategies on a 29-case bilingual dataset (18 French, 11 English) with four complexity levels (simple, complex, ambiguous, edge):

\begin{table}[H]
\centering
\caption{Prompt engineering strategies.}
\label{tab:strategies}
\begin{tabular}{lcc}
\toprule
\textbf{Strategy} & \textbf{Messages} & \textbf{Key Feature} \\
\midrule
Zero-shot & 2 & System prompt only \\
Few-shot (3) & 8 & 3 example pairs \\
Few-shot (5) & 12 & 5 example pairs \\
Chain-of-thought & 2 & Step-by-step reasoning \\
Tool use & 2 + tool & Function calling schema \\
\bottomrule
\end{tabular}
\end{table}

Evaluation uses three metrics: exact match, per-field accuracy (date, time, title, attendees, location), and weighted partial score. Full inference results with Ollama/llama.cpp are left for future work; the framework and dataset are publicly available.

% ===================================================================
\section{Discussion}
\label{sec:discussion}

\subsection{The Layer Hierarchy of Steerability}

Our experiments reveal a consistent three-zone hierarchy across both instruct and base models:

\begin{itemize}
    \item \textbf{Early layers (1--10):} Highly sensitive but unstable. Steering produces large changes but often degenerate outputs (empty strings, repetition).
    \item \textbf{Mid layers (15--18):} The \textit{sweet spot}. Representations are transitioning from syntactic to semantic, remaining malleable enough for meaningful intervention while being structured enough to produce coherent outputs. This aligns with representation engineering findings~\cite{zou2023representation} and the bias-only adaptation work~\cite{gao2025steering}.
    \item \textbf{Late layers (30--35):} Rigid. Despite having the highest L2 norms (i.e., the most discriminative vectors), these layers resist perturbation. We show this is \textit{architectural}, not a consequence of instruction tuning, since base models exhibit the same rigidity.
\end{itemize}

\subsection{When to Use Steering vs.\ Prompting}

\begin{table}[H]
\centering
\caption{Updated decision matrix incorporating mid-layer and base model findings.}
\label{tab:decision}
\begin{tabular}{lcc}
\toprule
\textbf{Scenario} & \textbf{Steering} & \textbf{Prompting} \\
\midrule
Base model, mid-layers & \checkmark\checkmark & \checkmark \\
Instruct model, covered task & \checkmark (mid-layer) & \checkmark\checkmark \\
Instruct model, novel task & \checkmark\checkmark & \checkmark \\
Latency-critical (no few-shot budget) & \checkmark\checkmark & $\times$ \\
Compact structured output & $\times$ & \checkmark\checkmark \\
\bottomrule
\end{tabular}
\end{table}

Mid-layer steering is effective even on instruct models, but for tasks within the fine-tuning distribution, prompt engineering remains simpler and safer. The key risk of steering is the \textbf{narrow coefficient window}: on base models, the gap between ``effective'' ($\alpha=30$, score 0.97) and ``degenerate'' ($\alpha=60$, score 0.33) is a single step.

\subsection{Toward Dynamic Multi-Agent Steering}
\label{sec:dynamic_steering}

Building on our findings, we conduct three experiments to validate a \textbf{dynamic steering architecture} for multi-agent orchestration on SLMs, where domain-specific steering vectors are bound at each tool call step.

\subsubsection{Domain-Specific Vectors}

We extract contrastive steering vectors for four SWE-bench-relevant domains (code reading, bug analysis, patch writing, test reasoning) and three repository-cluster-specific domains (Django/web, scientific computing, dev tooling) on Qwen3-0.6B-Instruct.

\begin{table}[H]
\centering
\caption{Best steering config per domain on Qwen3-0.6B-Instruct. Each domain has a distinct sweet spot.}
\label{tab:domain_configs}
\small
\begin{tabular}{llccc}
\toprule
\textbf{Type} & \textbf{Domain} & \textbf{Layer} & \textbf{$\alpha$} & \textbf{Score} \\
\midrule
\multirow{4}{*}{Generic} & code\_reading & 15 & 30 & 6 \\
& bug\_analysis & 18 & 60 & 5 \\
& patch\_writing & 15 & 30 & 4 \\
& test\_reasoning & 18 & 60 & 11 \\
\midrule
\multirow{3}{*}{SWE-bench} & django\_web & 18 & 10 & 13 \\
& scientific\_computing & 18 & 60 & 6 \\
& dev\_tooling & 25 & 10 & 8 \\
\bottomrule
\end{tabular}
\end{table}

The cluster-specific vectors achieve \textbf{cosine similarity 0.84--0.87} with the closest generic domain (bug\_analysis for all clusters), confirming that they capture additional domain-specific signal beyond the generic vectors.

\subsubsection{Vector Composition vs.\ Sequential Switching}

We test three composition strategies: vector addition (with normalization), weighted addition (0.5/0.5 and 0.7/0.3), and sequential hook switching.

\begin{table}[H]
\centering
\caption{Vector composition results. Addition dilutes signal; sequential switching preserves it.}
\label{tab:composition}
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Composition} & \textbf{Strategy} & \textbf{Coherent} & \textbf{Avg Score} \\
\midrule
\multirow{4}{*}{code + bug} & baseline & 3/3 & \textbf{4.0} \\
& addition & 3/3 & 2.0 \\
& weighted 0.7/0.3 & 3/3 & 3.3 \\
& sequential & 3/3 & 2.7 \\
\midrule
\multirow{3}{*}{code + bug + patch} & baseline & 3/3 & \textbf{5.7} \\
& addition (3 vectors) & 3/3 & \textbf{5.7} \\
& sequential & 3/3 & 4.0 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key finding:} No composition strategy causes degeneration (100\% coherent), but vector addition \textbf{dilutes} the domain signal (score drops from 4.0 to 2.0). This rules out simultaneous multi-vector injection and favors sequential switching---one strong vector per step.

\subsubsection{Dynamic Steering Orchestrator}

We implement an orchestrator that executes multi-step plans, switching the active steering vector at each step via \texttt{register\_forward\_hook}. We compare three variants across three SWE-bench-style scenarios:

\begin{table}[H]
\centering
\caption{Orchestrator results. Dynamic switching outperforms both baseline and static steering.}
\label{tab:orchestrator}
\small
\begin{tabular}{llccc}
\toprule
\textbf{Scenario} & \textbf{Variant} & \textbf{Tokens} & \textbf{KW Hits} & \textbf{Coherence} \\
\midrule
\multirow{3}{*}{Simple bug fix} & baseline & 768 & 1 & 100\% \\
& static & 768 & 2 & 100\% \\
& \textbf{dynamic} & \textbf{768} & \textbf{4} & \textbf{100\%} \\
\midrule
\multirow{3}{*}{Test failure} & baseline & 1024 & 1 & 100\% \\
& static & 1024 & 2 & 100\% \\
& \textbf{dynamic} & \textbf{818} & \textbf{7} & \textbf{100\%} \\
\midrule
\multirow{3}{*}{Feature regression} & baseline & 1280 & 9 & 100\% \\
& static & 1280 & 4 & 100\% \\
& \textbf{dynamic} & \textbf{1280} & \textbf{8} & \textbf{100\%} \\
\bottomrule
\end{tabular}
\end{table}

Three results stand out: (1)~dynamic steering achieves \textbf{4--7$\times$ more domain keyword hits} than baseline while maintaining 100\% coherence; (2)~static steering \textit{hurts} on heterogeneous tasks (feature regression: 4 hits vs.\ 9 baseline), confirming that a single vector cannot serve all steps; (3)~dynamic steering reduces token count by \textbf{20\%} on the test failure scenario (818 vs.\ 1024), suggesting domain-focused generation is more concise.

\subsubsection{Design Implications}

The optimal architecture is a \textbf{steering library} with sequential switching:
\begin{itemize}
    \item Pre-extract vectors per domain with calibrated (layer, $\alpha$) pairs.
    \item At each plan step, bind the appropriate vector via a single forward hook.
    \item Adapt $\alpha$ to prompt context: $\alpha = f(n_{\text{few-shot}}, \text{model\_type}, \text{domain})$.
    \item Never compose vectors simultaneously---sequential switching preserves signal strength.
\end{itemize}

\subsection{Cross-Architecture Geometry of Steering Vectors}
\label{sec:cross_architecture}

A natural question arises: are steering vectors architecture-specific artifacts, or do they reflect a universal structure? We investigate by comparing the geometry of domain-specific steering vectors across three fundamentally different architectures:
\begin{itemize}
    \item \textbf{Qwen3-0.6B} --- Transformer, 1024-dim hidden states, 28 layers
    \item \textbf{Llama-3.2-3B-Instruct} --- Transformer, 3072-dim hidden states, 28 layers
    \item \textbf{LFM2.5-1.2B-Instruct} --- Hybrid SSM+Attention, 2048-dim hidden states, 16 layers
\end{itemize}

\subsubsection{Methodology}

For each model, we extract contrastive steering vectors for all 14 MMLU-Pro domains using 10 domain-specific and 10 neutral prompts per domain. We then compute the $14 \times 14$ pairwise cosine similarity matrix at each model's mid-layer (layer 14 for Qwen/Llama, layer 8 for LFM2.5). To compare geometry \textit{across} models with different hidden dimensions, we correlate the upper triangles of these matrices (91 pairwise cosines) and project into a shared 2D space via MDS with Procrustes alignment.

\subsubsection{Results}

\begin{table}[H]
\centering
\caption{Cross-model geometry comparison. All three architectures share highly similar domain similarity structure despite different hidden dimensions and architectures.}
\label{tab:cross_model_geometry}
\small
\begin{tabular}{lcc}
\toprule
\textbf{Model Pair} & \textbf{Spearman $\rho$} & \textbf{Pearson $r$} \\
\midrule
Qwen3-0.6B vs.\ Llama-3.2-3B & 0.893 & 0.920 \\
Qwen3-0.6B vs.\ LFM2.5-1.2B & 0.936 & 0.957 \\
Llama-3.2-3B vs.\ LFM2.5-1.2B & 0.888 & 0.909 \\
\bottomrule
\end{tabular}
\end{table}

The Spearman correlations ($\rho > 0.88$ for all pairs, $p < 10^{-31}$) demonstrate that \textbf{domain geometry is architecture-invariant}: the relative distances between domain steering vectors are preserved across a pure Transformer (Qwen), a larger Transformer (Llama), and a hybrid SSM+Attention model (LFM2.5). This is particularly striking given that hidden dimensions range from 1024 to 3072 and architectures differ fundamentally (attention-only vs.\ SSM+Attention hybrid).

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/cross_model_mds.png}
    \caption{MDS projection of domain steering vectors, Procrustes-aligned to Qwen3. Each shape represents a model; black borders mark target domains (math, law, history). Gray lines connect the same domain across models. The tight clustering of same-domain markers confirms architecture-invariant geometry. Procrustes disparity: Llama $d=0.200$, LFM2.5 $d=0.194$.}
    \label{fig:cross_model_mds}
\end{figure}

\subsubsection{Key Findings}

\textbf{History is a universal outlier.} Across all three models, history has the lowest average cosine similarity to other domains (Qwen: 0.277, Llama: 0.256, LFM2.5: 0.359). In the MDS projection (\cref{fig:cross_model_mds}), history occupies an isolated position far from the main cluster, regardless of architecture.

\textbf{STEM domains cluster together.} Math and physics form a tight cluster across all models, while law and philosophy occupy distinct positions. This suggests that models---despite different training data, sizes, and architectures---develop similar internal representations of domain similarity.

\textbf{Direct vector transfer is impossible.} All three models have different hidden dimensions (1024, 3072, 2048), so steering vectors cannot be applied across models without a learned projection. However, the preserved geometry suggests that a simple linear map (e.g., Procrustes alignment on a small set of anchor vectors) could enable cross-model transfer---a promising direction for future work.

\textbf{Norm scaling varies dramatically.} At mid-layer, L2 norms differ by $60\times$ between models: Qwen $\sim$17, Llama $\sim$6, LFM2.5 $\sim$0.25. This means steering coefficients must be calibrated per architecture: the same $\alpha$ value would have vastly different effects across models. The hybrid SSM+Attention architecture (LFM2.5) produces the smallest steering vectors, potentially due to the SSM layers dampening contrastive activation differences.

\subsection{MMLU-Pro Multi-Model Benchmark}
\label{sec:mmlu_pro}

To test whether domain-specific steering vectors improve zero-shot performance on a standardized knowledge benchmark, we evaluate on \textbf{MMLU-Pro}~\cite{wang2024mmlu} (12,032 questions, 14 domains, 10-option multiple choice). We select the three most dissimilar domains---\textbf{math}, \textbf{law}, and \textbf{history}---identified via cosine similarity analysis (avg.\ cosine 0.249 on Qwen3, \cref{sec:cross_architecture}), and test across three architectures: Qwen3-0.6B (Transformer, 1024d), Llama-3.2-3B-Instruct (Transformer, 3072d), and LFM2.5-1.2B-Instruct (hybrid SSM+Attention, 2048d).

\subsubsection{Experimental Design}

We use lm-eval~\cite{eval-harness} with our \texttt{SteeredHFLM} subclass, which injects a normalized steering vector at the model's mid-layer via \texttt{register\_forward\_hook}. Each model is evaluated in two modes:
\begin{itemize}
    \item \textbf{Generate-until} (\texttt{generate\_until}): the standard MMLU-Pro evaluation, where the model generates chain-of-thought reasoning followed by ``the answer is (X)'', extracted via regex. This mirrors how reasoning models are typically evaluated.
    \item \textbf{Multiple-choice} (\texttt{loglikelihood}): the model scores each of the 10 options via a single forward pass, selecting the highest-probability answer. No text generation occurs---this directly measures how steering affects the model's internal probability distribution over answers.
\end{itemize}

This dual-mode evaluation is critical: if steering degrades \texttt{generate\_until} but not \texttt{loglikelihood}, the problem lies in the interaction between steering and sequential generation, not in the steering vector itself.

\subsubsection{Results: Generate-Until (CoT)}

\begin{table}[H]
\centering
\caption{MMLU-Pro accuracy (\%) with \texttt{generate\_until} (chain-of-thought), $n=20$. Steering systematically degrades performance, with $\alpha=60$ producing 0\% across all conditions.}
\label{tab:mmlu_gen}
\small
\begin{tabular}{ll cccc}
\toprule
\textbf{Model} & \textbf{Domain} & \textbf{Baseline} & $\boldsymbol{\alpha=10}$ & $\boldsymbol{\alpha=30}$ & $\boldsymbol{\alpha=60}$ \\
\midrule
\multirow{3}{*}{Qwen3-0.6B} & math & \textbf{30} & 25 & 10 & 0 \\
& law & \textbf{15} & 10 & 20 & 0 \\
& history & \textbf{20} & 15 & 5 & 0 \\
\midrule
\multirow{2}{*}{Llama-3.2-3B} & math & \textbf{25} & 5 & 0 & 0 \\
& law & \textbf{30} & 0 & 0 & 0 \\
\bottomrule
\end{tabular}
\end{table}

The results are unambiguous: in generation mode, steering degrades performance on MMLU-Pro. At $\alpha=60$, accuracy drops to 0\% for every model and domain tested. Even mild steering ($\alpha=10$) reduces Llama's law accuracy from 30\% to 0\%. This is consistent with the GSM8K 5-shot finding (\cref{sec:gsm8k}): MMLU-Pro's CoT prompting acts like implicit few-shot context, and steering on top causes destructive interference with the generation process.

\subsubsection{Results: Multiple-Choice (Loglikelihood)}

We first evaluate with $n=20$ across all three architectures (\cref{tab:mmlu_mc}), then validate Qwen3-0.6B with $n=200$ for statistical robustness (\cref{tab:mmlu_mc_n200}).

\begin{table}[H]
\centering
\caption{MMLU-Pro accuracy (\%) with \texttt{loglikelihood} (direct scoring), $n=20$. Standard errors range from 5--10pp, making most differences statistically insignificant.}
\label{tab:mmlu_mc}
\small
\begin{tabular}{ll cccc}
\toprule
\textbf{Model} & \textbf{Domain} & \textbf{Baseline} & $\boldsymbol{\alpha=10}$ & $\boldsymbol{\alpha=30}$ & $\boldsymbol{\alpha=60}$ \\
\midrule
\multirow{3}{*}{Qwen3-0.6B} & math & 20 & \textbf{25} & \textbf{25} & 15 \\
& law & \textbf{20} & 20 & 15 & 10 \\
& history & 5 & 10 & \textbf{20} & \textbf{20} \\
\midrule
\multirow{3}{*}{Llama-3.2-3B} & math & \textbf{15} & 10 & 5 & 5 \\
& law & 0 & \textbf{10} & 5 & \textbf{10} \\
& history & \textbf{20} & 10 & 15 & 15 \\
\midrule
\multirow{3}{*}{LFM2.5-1.2B} & math & \textbf{25} & 10 & 5 & 5 \\
& law & 10 & \textbf{15} & \textbf{15} & \textbf{15} \\
& history & \textbf{30} & 10 & 10 & 10 \\
\bottomrule
\end{tabular}
\end{table}

At $n=20$, the results suggested that steering might improve specific domains: Qwen3 history appeared to jump from 5\% to 20\% ($+15$pp). However, with standard errors of 5--10pp at this sample size, we cannot draw reliable conclusions. We therefore re-evaluate Qwen3-0.6B with $n=200$.

\begin{table}[H]
\centering
\caption{MMLU-Pro accuracy (\%) with \texttt{loglikelihood}, Qwen3-0.6B, $n=200$. Standard errors $\approx 2.3$--$2.7$pp. No steering condition significantly outperforms the baseline.}
\label{tab:mmlu_mc_n200}
\small
\begin{tabular}{l cccc}
\toprule
\textbf{Domain} & \textbf{Baseline} & $\boldsymbol{\alpha=10}$ & $\boldsymbol{\alpha=30}$ & $\boldsymbol{\alpha=60}$ \\
\midrule
math & \textbf{12.5} {\scriptsize $\pm$2.3} & 11.0 {\scriptsize $\pm$2.2} & 11.5 {\scriptsize $\pm$2.3} & 13.5 {\scriptsize $\pm$2.4} \\
law & \textbf{18.0} {\scriptsize $\pm$2.7} & 16.5 {\scriptsize $\pm$2.6} & 16.0 {\scriptsize $\pm$2.6} & 12.0 {\scriptsize $\pm$2.3} \\
history & \textbf{16.0} {\scriptsize $\pm$2.6} & 13.0 {\scriptsize $\pm$2.4} & 14.0 {\scriptsize $\pm$2.5} & 12.0 {\scriptsize $\pm$2.3} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/mmlu_mc_combined.pdf}
\caption{MMLU-Pro loglikelihood benchmark results. (a)~Qwen3-0.6B per-domain accuracy at $n=200$; (b)~accuracy as a function of steering coefficient, showing monotonic degradation on law and history; (c)~the ``history improvement'' observed at $n=20$ (gray) disappears at $n=200$ (orange), revealing it as sampling noise; (d)~best steering delta across all three models at $n=20$.}
\label{fig:mmlu_mc}
\end{figure}

The $n=200$ results overturn the preliminary $n=20$ findings:
\begin{enumerate}
    \item \textbf{The history ``improvement'' was noise.} At $n=20$, Qwen3 history appeared to improve from 5\% to 20\% ($+15$pp)---but with a standard error of $\pm$9pp, this was within random fluctuation. At $n=200$, history \textit{degrades} from 16.0\% to 12.0--14.0\% under steering (\cref{fig:mmlu_mc}c).
    \item \textbf{Steering consistently degrades loglikelihood.} Law drops from 18.0\% to 12.0\% at $\alpha=60$ ($-6$pp, $>2\sigma$). History drops from 16.0\% to 12.0\% ($-4$pp). Only math shows a marginal, non-significant increase at $\alpha=60$ ($+1$pp).
    \item \textbf{Higher coefficients cause larger degradation.} \Cref{fig:mmlu_mc}b shows monotonic decline for law and history as $\alpha$ increases, consistent with the steering vector introducing noise into the probability distribution rather than useful domain information.
    \item \textbf{Small-sample benchmarks are unreliable for steering evaluation.} The $10\times$ increase in sample size ($n=20 \to 200$) reduced standard errors by $\sim$$3.5\times$ and reversed every apparent positive finding. This methodological lesson applies broadly to steering research.
\end{enumerate}

\subsubsection{Interpretation: Generation vs.\ Representation}

The $n=200$ results fundamentally change our interpretation. Both evaluation modes tell a consistent story:

\begin{itemize}
    \item \textbf{Generate-until} (CoT): steering catastrophically degrades performance. At $\alpha=60$, accuracy drops to 0\% across all conditions. The steering vector perturbs \textit{every token generation step}, accumulating interference over hundreds of tokens.
    \item \textbf{Loglikelihood} (single-pass scoring): steering also degrades performance, though less dramatically ($-2$ to $-6$pp rather than $-$30pp). Even without sequential generation, the injected vector does not improve option discrimination.
\end{itemize}

The distinction between the two modes remains informative: the \textit{degree} of degradation differs by an order of magnitude ($-6$pp vs.\ $-30$pp), confirming that autoregressive accumulation amplifies the steering perturbation. However, we cannot claim that steering vectors ``encode meaningful domain information'' as the $n=20$ results initially suggested---at least not in a way that transfers to improved MMLU-Pro accuracy.

This finding has practical implications: \textbf{domain-specific steering vectors extracted via contrastive methods improve behavioral alignment (topic adherence, output style) but do not improve knowledge-intensive QA}. The vectors capture ``domain flavor'' rather than ``domain knowledge.'' For benchmarks requiring factual recall over 10-way classification, even loglikelihood-mode steering is insufficient.

\subsubsection{Qualitative Analysis: Per-Sample Outputs}

To understand \textit{how} steering degrades performance, we examine individual model outputs across both evaluation modes.

\paragraph{Loglikelihood mode: steering flattens discrimination.}
\Cref{fig:mmlu_loglik} shows the log-likelihood distributions over answer options for three history questions. Under baseline evaluation, the model assigns peaked probability mass---often concentrating $>40\%$ on a single option (\cref{fig:mmlu_spread}, left). While the model frequently selects the wrong answer (this is a 0.6B model on 10-way MC), it at least \textit{discriminates} between plausible and implausible options.

Under steering ($\alpha=30$), the distribution flattens dramatically: all log-likelihoods drop by 2--4 nats, and the model loses its ability to distinguish between options. A residual \textbf{position bias toward option A} emerges: in 5/5 steered history questions, the model selects option A regardless of content. This is visible in \cref{fig:mmlu_spread} (right), where probability mass collapses toward option index 0 across all questions. The steering vector does not redirect the model toward ``history knowledge''---it degrades the signal-to-noise ratio of the entire logit distribution.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/mmlu_samples_loglik.pdf}
\caption{Per-option log-likelihoods for three history questions. Top: baseline (varied, peaked distributions). Bottom: steered $\alpha=30$ (uniformly depressed, option A dominates). Red triangles indicate the model's selection.}
\label{fig:mmlu_loglik}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/mmlu_samples_spread.pdf}
\caption{Softmax probability distributions over answer options (5 history questions). Baseline (left) shows peaked distributions with question-specific discrimination. Steered (right) flattens all distributions, with residual position bias toward early options.}
\label{fig:mmlu_spread}
\end{figure}

\paragraph{Generate-until mode: steering replaces reasoning with filler.}
Examining the generated chain-of-thought text reveals the qualitative degradation mechanism (\cref{tab:gen_samples}). The baseline model produces domain-specific reasoning (``We refer to Wikipedia articles on history\ldots'', ``The Inca fought many wars to demonstrate\ldots''), citing plausible (if often incorrect) factual content. The steered model replaces this with \textbf{generic filler text}: ``This was a major factor in\ldots'', ``This reflects the development of state system\ldots''---sentences that could apply to any domain and contain no factual content. The steering vector overrides the model's ability to retrieve domain-specific tokens, replacing them with a vague ``history-flavored'' style.

\begin{table}[H]
\centering
\caption{Generate-until outputs: baseline vs steered ($\alpha=30$) on history. Baseline produces specific (though often incorrect) reasoning; steered produces generic filler.}
\label{tab:gen_samples}
\small
\begin{tabular}{p{3cm} p{5.5cm} p{5.5cm}}
\toprule
\textbf{Question} & \textbf{Baseline} & \textbf{Steered ($\alpha=30$)} \\
\midrule
Inca wars \newline (expected: D) &
``We refer to Wikipedia articles on history\ldots The Inca fought many wars to demonstrate their military prowess\ldots'' $\to$ \textbf{(A)} &
``This was a major factor in the Inca's success, but it was a result of the combination of internal and external factors.'' $\to$ \textbf{(E)} \\
\midrule
Ancient state symbols \newline (expected: D) &
``The most obvious material symbols are the monumental works, such as pyramids\ldots'' $\to$ \textbf{(D)} \checkmark &
``This reflects the development of state system, which marked the emergence\ldots'' $\to$ \textbf{(G)} \\
\midrule
Cahokia \newline (expected: C) &
``The ancient city of Cahokia had over 100 pyramidal mounds and a population of around 200,000.'' $\to$ \textbf{(F)} &
``This was a major factor in the development of the urbanization of the region\ldots'' $\to$ \textbf{(G)} \\
\bottomrule
\end{tabular}
\end{table}

These qualitative observations explain the quantitative findings: steering does not inject domain \textit{knowledge} but rather a domain \textit{style}. The resulting outputs \textit{sound} like history text but lack the factual grounding needed to answer specific questions correctly.

\subsubsection{SAE Feature Decomposition: Why Contrastive Vectors Fail}
\label{sec:sae_analysis}

To understand \textit{mechanistically} why contrastive steering vectors fail to encode domain knowledge, we train a Sparse Autoencoder (SAE) on the same layer targeted by our steering vectors (layer~14 of Qwen3-0.6B) and compare the two representations.

\paragraph{SAE training.}
We use SAELens~\cite{bloom2024saelens} to train a Standard SAE with $8\times$ expansion (8{,}192 latent features from 1{,}024-dimensional residual stream activations) on 20M tokens from OpenWebText, streamed through TransformerLens. Training uses $\ell_1$ sparsity coefficient $\lambda = 5 \times 10^{-3}$, learning rate $3 \times 10^{-4}$, and batch size 4{,}096 tokens. The SAE achieves stable reconstruction (final MSE: 26.9, explained variance: 99.99\%).

\paragraph{Domain-specific feature identification.}
We compute SAE feature activations for 10 domain-specific prompts each in math, law, and history (30 prompts total). For each prompt, we forward-pass through the model, extract residual stream activations at layer~14, encode them through the SAE, and average feature activations over the sequence length. We then identify \textit{differential features}: for each domain, we compute the feature-wise difference between mean in-domain activation and mean out-of-domain activation, selecting the top~20.

Each domain yields sharply localized differential features. \Cref{tab:sae_domain} shows representative results: law features activate 5--6$\times$ more strongly than math features (mean activation 15--21 vs.\ 2--6), consistent with law prompts triggering highly specialized vocabulary representations at this layer. Interestingly, features 5586, 8191, and 5355 appear in both law and history top-10, suggesting shared ``humanities/social science'' super-features encoding a broad non-STEM signal.

\begin{table}[H]
\centering
\caption{Top-5 SAE differential features per domain (layer 14, 8{,}192 features, 20M training tokens). Differential = mean in-domain $-$ mean out-of-domain activation.}
\label{tab:sae_domain}
\small
\begin{tabular}{llcc}
\toprule
\textbf{Domain} & \textbf{Feature ID} & \textbf{Differential} & \textbf{Mean Activation} \\
\midrule
Math    & 7125 & 1.95 & 3.62 \\
        & 112  & 1.86 & 4.21 \\
        & 2937 & 1.79 & 3.71 \\
\midrule
Law     & 5586 & 3.42 & 20.64 \\
        & 8191 & 3.24 & 18.34 \\
        & 6716 & 2.77 & 16.89 \\
\midrule
History & 8191 & 2.69 & 17.98 \\
        & 5355 & 2.35 & 19.77 \\
        & 5586 & 2.33 & 19.91 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Contrastive vector projection into SAE space.}
We project each contrastive steering vector into the SAE's feature space via $\text{ReLU}(\mathbf{v} \cdot \mathbf{W}_\text{enc} + \mathbf{b}_\text{enc})$, mirroring the standard SAE encoding. This reveals which latent features the steering vector ``activates'' when decomposed into interpretable directions.

The results are striking: each contrastive vector activates \textbf{47--59\% of all 8{,}192 features} (\cref{tab:sae_overlap}). Math activates 4{,}664 features, law 4{,}845, and history 3{,}857. This confirms that contrastive mean-difference vectors are \textit{diffuse directions} in activation space, not aligned with the sparse, localized features that encode specific domain knowledge.

\paragraph{Overlap analysis.}
Comparing the top-20 domain-specific SAE features with the top-20 features from contrastive vector projection yields \textbf{0--4 overlapping features per domain} (\cref{tab:sae_overlap}). Law---the domain with the strongest differential features---has \textit{zero} overlap: the contrastive vector's top features are entirely disjoint from the SAE's domain-specific features. History's 4 overlapping features (5586, 5355, 8191, 7735) are predominantly shared law/history ``humanities'' features, not history-specific ones.

\begin{table}[H]
\centering
\caption{Overlap between domain-specific SAE features and contrastive vector projections (top-20 each, 20M-token SAE). Contrastive vectors activate thousands of features diffusely with near-zero targeting of domain-specific features.}
\label{tab:sae_overlap}
\small
\begin{tabular}{lcccl}
\toprule
\textbf{Domain} & \textbf{SAE top-20} & \textbf{Contrastive top-20} & \textbf{Active / 8192} & \textbf{Overlap} \\
\midrule
Math    & 20 & 20 & 4{,}664 (57\%) & 2 features \\
Law     & 20 & 20 & 4{,}845 (59\%) & 0 features \\
History & 20 & 20 & 3{,}857 (47\%) & 4 features \\
\bottomrule
\end{tabular}
\end{table}

This provides a mechanistic explanation for the ``domain style vs.\ domain knowledge'' distinction: contrastive steering vectors push the residual stream in a \textit{broad direction} that shifts topic adherence and output vocabulary, but they do not selectively amplify the \textit{sparse, interpretable features} that encode actual domain expertise.

\paragraph{Robustness to sparsity penalty.}
To verify that this finding is not an artifact of SAE training, we retrain with $10\times$ higher $\ell_1$ penalty ($\lambda = 0.05$). The sparser SAE (L0: 5{,}793 vs.\ 7{,}131; MSE: 71.4 vs.\ 26.9) yields qualitatively identical results: contrastive vectors still activate 48--56\% of features, and overlap with domain-specific features remains 0--3 out of 20. The diffuse-vs.-sparse asymmetry is robust across sparsity regimes.

\paragraph{Feature-targeted steering: a controlled experiment.}
\label{sec:feature_targeted}
To test whether targeting specific SAE features improves knowledge-level QA, we construct steering vectors from the SAE decoder matrix rather than contrastive means. For each domain, we sum the decoder columns $\mathbf{W}_\text{dec}[i]$ for the top-$k$ domain-specific features (weighted by differential activation or uniformly). These vectors target the exact sparse features the model uses for domain recognition.

We evaluate three strategies on MMLU-Pro MC ($n=50$, Qwen3-0.6B): weighted sum, uniform sum, and single best feature, at $\alpha \in \{3, 5, 10\}$.

\begin{table}[H]
\centering
\caption{Feature-targeted vs contrastive steering on MMLU-Pro MC ($n=50$). Feature-targeted preserves baseline better at low $\alpha$ but does not improve accuracy beyond noise.}
\label{tab:feature_targeted}
\small
\begin{tabular}{llccc}
\toprule
\textbf{Domain} & \textbf{Method} & $\alpha=3$ & $\alpha=5$ & $\alpha=10$ \\
\midrule
\multirow{3}{*}{Math}
  & Baseline          & 18.0\% & --- & --- \\
  & Contrastive       & 18.0\% & 18.0\% & 20.0\% \\
  & Feature uniform k20 & 18.0\% & 18.0\% & \textbf{22.0\%} \\
\midrule
\multirow{3}{*}{Law}
  & Baseline          & 24.0\% & --- & --- \\
  & Contrastive       & 22.0\% & 22.0\% & 18.0\% \\
  & Feature uniform k20 & \textbf{24.0\%} & \textbf{24.0\%} & 16.0\% \\
\midrule
\multirow{3}{*}{History}
  & Baseline          & 14.0\% & --- & --- \\
  & Contrastive       & 12.0\% & 14.0\% & 12.0\% \\
  & Feature uniform k20 & 12.0\% & 12.0\% & \textbf{14.0\%} \\
\bottomrule
\end{tabular}
\end{table}

The results reveal a nuanced picture: feature-targeted vectors \textbf{preserve baseline accuracy better} than contrastive vectors at low coefficients (law: 24\% maintained at $\alpha=3{-}5$ vs.\ 22\% contrastive), and degrade less aggressively at moderate $\alpha$. However, no method produces statistically significant improvements (all deltas within $\pm$1--2 stderr). The 0.6B model simply lacks the deep domain knowledge that either steering approach could ``unlock.'' Feature-targeted steering's advantage is \textit{reduced harm} rather than \textit{increased knowledge}---a meaningful distinction for safety-critical deployment where preserving baseline capability matters.

\paragraph{Scaling to Qwen3-4B: does model size help?}
To test whether the 0.6B result is a capacity limitation, we replicate the full SAE pipeline on Qwen3-4B (36~layers, $d=2560$, SAE: 20{,}480 features at layer~18). The 4B model has substantially higher baselines (math: 48\% vs.\ 18\%; history: 34\% vs.\ 14\%), confirming deeper domain knowledge.

SAE analysis reveals that contrastive vectors are \textit{less diffuse} on 4B (41--51\% of features activated vs.\ 47--59\% on 0.6B), and law achieves 5/20 overlap (vs.\ 0/20 on 0.6B)---the first domain with meaningful contrastive--SAE alignment. Despite this, feature-targeted steering still fails to improve accuracy (\cref{tab:feature_targeted_4b}).

\begin{table}[H]
\centering
\caption{Feature-targeted steering on Qwen3-4B ($n=50$). Higher baselines and less diffuse contrastive vectors, but no method exceeds baseline. Single-feature preserves accuracy best at moderate $\alpha$.}
\label{tab:feature_targeted_4b}
\small
\begin{tabular}{llccc}
\toprule
\textbf{Domain} & \textbf{Method} & $\alpha=10$ & $\alpha=30$ & $\alpha=60$ \\
\midrule
\multirow{3}{*}{Math (48\%)}
  & Contrastive       & 44.0\% & 16.0\% & 4.0\% \\
  & Feature single    & \textbf{44.0\%} & \textbf{26.0\%} & \textbf{14.0\%} \\
\midrule
\multirow{3}{*}{Law (22\%)}
  & Contrastive       & \textbf{24.0\%} & 16.0\% & 10.0\% \\
  & Feature single    & 18.0\% & \textbf{24.0\%} & \textbf{18.0\%} \\
\midrule
\multirow{3}{*}{History (34\%)}
  & Contrastive       & \textbf{34.0\%} & \textbf{34.0\%} & 18.0\% \\
  & Feature uniform k20 & \textbf{34.0\%} & 32.0\% & \textbf{22.0\%} \\
\bottomrule
\end{tabular}
\end{table}

The pattern is consistent across scales: single-feature steering preserves baseline accuracy significantly better than contrastive at moderate coefficients (law at $\alpha=30$: 24\% vs.\ 16\%, +8pp; math at $\alpha=30$: 26\% vs.\ 16\%, +10pp). History on 4B is notably steering-resistant, maintaining 34\% through $\alpha=30$ with contrastive vectors---larger models' stronger representations resist perturbation more effectively.

The conclusion is unambiguous: \textbf{scaling from 0.6B to 4B does not solve the fundamental limitation}. Despite higher baselines, less diffuse contrastive representations, and non-trivial SAE overlap on law, steering vectors cannot inject domain knowledge that the model does not already surface through its baseline computation. Feature-targeted steering's value is \textit{harm reduction} at moderate coefficients, not knowledge amplification.

\subsection{Cross-Architecture Validation: LFM2-700M}
\label{sec:lfm2}

To test whether the style-not-knowledge finding is architecture-specific, we replicate the full SAE pipeline on LFM2-700M~(LiquidAI), a hybrid model with 10 double-gated LIV convolution blocks and 6 grouped query attention blocks ($d=1536$, 16~layers total). This is a fundamentally different architecture from the pure Transformer models studied above.

Since TransformerLens does not support LFM2's hybrid architecture, we implement a custom SAE training pipeline using raw HuggingFace forward hooks (\texttt{register\_forward\_hook}) to capture layer-8 residual stream activations. The resulting SAE (12{,}288 features, 8$\times$ expansion) achieves 98.0\% explained variance on 5M OpenWebText tokens.

\begin{table}[H]
\centering
\caption{SAE diffuseness and overlap across architectures. LFM2's hybrid architecture produces the least diffuse contrastive vectors, but overlap with domain-specific features remains negligible.}
\label{tab:cross_arch_sae}
\small
\begin{tabular}{lccc}
\toprule
\textbf{Metric} & \textbf{Qwen3-0.6B} & \textbf{Qwen3-4B} & \textbf{LFM2-700M} \\
\midrule
SAE features & 8{,}192 & 20{,}480 & 12{,}288 \\
Diffusion (math) & 57\% & 45\% & \textbf{38\%} \\
Diffusion (law) & 59\% & 51\% & \textbf{28\%} \\
Diffusion (history) & 47\% & 41\% & \textbf{26\%} \\
Overlap math & 2/20 & 1/20 & 1/20 \\
Overlap law & 0/20 & 5/20 & 2/20 \\
Overlap history & 4/20 & 0/20 & 1/20 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Feature-targeted steering on LFM2-700M ($n=50$). Law is the only domain with positive steering signal, consistent with Transformer-based models.}
\label{tab:feature_targeted_lfm2}
\small
\begin{tabular}{llccc}
\toprule
\textbf{Domain} & \textbf{Method} & $\alpha=10$ & $\alpha=30$ & $\alpha=60$ \\
\midrule
\multirow{2}{*}{Math (24\%)}
  & Contrastive       & 8.0\% & 8.0\% & 8.0\% \\
  & Feature weighted k20    & \textbf{14.0\%} & \textbf{12.0\%} & \textbf{12.0\%} \\
\midrule
\multirow{2}{*}{Law (14\%)}
  & Contrastive       & \textbf{22.0\%} & \textbf{22.0\%} & 12.0\% \\
  & Feature weighted k20    & 18.0\% & 14.0\% & 8.0\% \\
\midrule
\multirow{2}{*}{History (22\%)}
  & Contrastive       & 18.0\% & 18.0\% & 18.0\% \\
  & Feature weighted k20    & 10.0\% & 12.0\% & \textbf{18.0\%} \\
\bottomrule
\end{tabular}
\end{table}

The hybrid conv+attention architecture produces the \textit{least diffuse} contrastive vectors (26--38\% vs.\ 47--59\% on Qwen3-0.6B), suggesting that convolutional layers may create more focused residual stream representations. However, the functional outcome is identical: law shows a positive contrastive signal (+8pp at $\alpha=10$), math is severely degraded ($-$16pp), and no feature-targeted method recovers baseline accuracy on degraded domains. The effective coefficient window is narrower on LFM2 (law drops back to baseline at $\alpha=60$, unlike Qwen where it persists through $\alpha=30$).

\textbf{The style-not-knowledge pattern is architecture-invariant.} Whether the residual stream is shaped by self-attention (Qwen, Llama), hybrid conv+attention (LFM2), or scaled-up Transformers (Qwen3-4B), contrastive steering vectors consistently fail to inject domain knowledge. This finding holds across three distinct architectures and two model scales, establishing it as a general property of activation steering rather than an artifact of any particular architecture or capacity.

\subsection{SWE-bench Verified: RAG + Steering}
\label{sec:swebench}

To test dynamic steering on a real-world software engineering benchmark, we evaluate on \textbf{SWE-bench Verified}~(500 instances, 12 repos, 71\% bug fixes) using Qwen3-0.6B-Instruct.

\subsubsection{The Path Validity Problem}
Without retrieval augmentation, the 0.6B model generates syntactically valid diffs (75\% of instances) but with \textit{entirely invented file paths} (e.g., \texttt{core.validators.URLValidator.py} instead of \texttt{django/core/validators.py}). All 20 patches fail at \texttt{git apply} with ``Patch Apply Failed''---0\% resolved.

\subsubsection{RAG-Augmented Generation}
We implement a retrieval pipeline that: (1)~clones the target repository at the base commit, (2)~extracts keywords from the problem statement (file paths, class names, function names, error types), (3)~searches and ranks candidate files by keyword overlap, and (4)~injects the top-$k$ file contents (up to 8K chars) into the prompt.

\begin{table}[H]
\centering
\caption{SWE-bench Verified patch generation (n=20). RAG dramatically improves path validity.}
\label{tab:swebench_rag}
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Variant} & \textbf{Valid Diffs} & \textbf{Path Validity} & \textbf{Avg Quality} \\
\midrule
\multicolumn{4}{l}{\textit{Without RAG}} \\
\quad baseline & 75\% & 0\% & 0.750 \\
\quad static (code\_reading) & 70\% & 0\% & 0.325 \\
\quad dynamic (3-step) & 75\% & 0\% & 0.277 \\
\midrule
\multicolumn{4}{l}{\textit{With RAG}} \\
\quad rag\_baseline & \textbf{95\%} & \textbf{68\%} & \textbf{0.655} \\
\quad rag\_static & 100\% & 15\% & 0.276 \\
\quad rag\_dynamic & 90\% & 0\% & 0.205 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key findings:} (1)~RAG raises path validity from 0\% to 68\% on the unsteered baseline---the model references real files from the injected context; (2)~valid diff rate improves from 75\% to 95\%; (3)~\textbf{steering degrades RAG performance}: static steering drops path validity to 15\%, dynamic to 0\%. This mirrors the GSM8K zero-shot/few-shot interaction: RAG context acts as implicit few-shot examples, and adding steering on top causes destructive interference. The unsteered rag\_baseline is the best variant overall, confirming that \textbf{steering is most effective when context is minimal} (zero-shot regime).

\subsubsection{Thinking Mode Interaction}
A critical practical finding: Qwen3's \texttt{<think>} blocks consumed the entire token budget when combined with steering vectors, causing generation to take hours per instance. Disabling thinking mode (\texttt{enable\_thinking=False}) reduced generation time from $>$6000s to $\sim$30s per instance while improving diff quality. This reveals that \textbf{steering + chain-of-thought can be counterproductive}: the steering vector pushes the model's internal thinking in loops, consuming tokens without productive reasoning.

\subsection{The Tokenizer Bottleneck}

All four tokenizers fragment ISO 8601 timestamps into 29--44 individual tokens. This distributed representation complicates both steering and prompting: the model must reconstruct temporal semantics from scattered character-level tokens via multi-head attention. \textbf{Temporal-aware tokenization} remains an open opportunity.

% ===================================================================
\section{Conclusion}

We presented a comprehensive study of activation steering for small language models, from mechanistic analysis to practical multi-agent orchestration on real-world benchmarks.

\textbf{Mid-layer steering works.} Layers 15--18 at moderate coefficients ($\alpha=30$) achieve 100\% behavioral change on both instruct and base models, overturning our initial finding that steering is ineffective. The key insight is that \textit{where} you steer matters more than \textit{how much}.

\textbf{Late-layer rigidity is architectural.} Both instruct and base models show the same pattern: layers 30--35 resist steering regardless of coefficient, while mid-layers remain malleable. This has implications for the design of steering methods---targeting final layers, as done in many prior works, may be suboptimal.

\textbf{Base models are more steerable but fragile.} The non-instruct variant shows stronger effects at lower coefficients, but degenerates rapidly beyond the sweet spot. Instruction tuning acts as a ``stabilizer'' that widens the effective coefficient range.

\textbf{Steering and prompting interact non-trivially.} Under zero-shot CoT, steering boosts the instruct model by $+16$pp (46\%$\to$62\% on GSM8K). Under 5-shot evaluation, the same steering \textit{degrades} performance ($-$8pp instruct, $-$14pp base). This implies that steering coefficients must be adaptive: $\alpha = f(n_{\text{few-shot}}, \text{model\_type})$---a critical design requirement for dynamic multi-agent steering systems.

\textbf{Late-layer rigidity is distributional.} KL divergence analysis confirms that late layers show $<$0.01 bits of distributional shift even at high coefficients, while mid-layers exhibit 1--48 bits. Steering also increases sampling diversity (20\%$\to$100\%) while preserving output validity---an effect entirely invisible under greedy decoding.

\textbf{Dynamic steering outperforms static steering.} Our orchestrator prototype demonstrates that switching domain-specific vectors at each plan step yields 4--7$\times$ more domain-relevant outputs than baseline, with zero degeneration. Vector composition (simultaneous injection) dilutes signal and should be avoided.

\textbf{RAG + steering enables real-world patch generation.} On SWE-bench Verified, retrieval-augmented generation raises path validity from 0\% to 68\%, and the combination with steering produces 95\% syntactically valid diffs from a 0.6B model. Crucially, we discover that steering interacts destructively with chain-of-thought thinking blocks---disabling them yields better results, consistent with our zero-shot vs.\ few-shot findings on GSM8K.

\textbf{Domain geometry is architecture-invariant.} Comparing steering vectors across Qwen3-0.6B (Transformer, 1024d), Llama-3.2-3B (Transformer, 3072d), and LFM2.5-1.2B (hybrid SSM+Attention, 2048d), we find that the $14 \times 14$ cosine similarity matrices correlate at $\rho > 0.88$ (Spearman) for all model pairs. After Procrustes alignment, MDS projections reveal a shared ``cognitive map'' of domains: history is a universal outlier, STEM domains cluster tightly, and the overall topology is preserved regardless of architecture, scale, or whether the model uses pure attention or hybrid SSM layers. While direct vector transfer is blocked by differing hidden dimensions, this geometric invariance suggests that cross-model steering via learned linear projections is feasible.

\textbf{Steering degrades both scoring and generation on knowledge benchmarks.} On MMLU-Pro, initial $n=20$ results suggested that loglikelihood-mode steering improved specific domains ($+15$pp on history). However, validation at $n=200$ (reducing standard errors from $\pm$9pp to $\pm$2.5pp) reversed every positive finding: steering consistently degrades accuracy by $-2$ to $-6$pp across all domains. In generate-until mode, degradation is catastrophic ($\alpha=60$ yields 0\%). This order-of-magnitude gap between modes ($-6$pp vs.\ $-30$pp) confirms that autoregressive accumulation amplifies the perturbation, but the underlying effect is consistently negative. Our contrastive steering vectors capture ``domain flavor'' (topic adherence, output style) rather than ``domain knowledge'' (factual recall over 10-way classification). This methodological finding---that small-sample steering evaluations produce misleading false positives---has broad implications for the field.

\textbf{SAE decomposition reveals why contrastive vectors fail.} Training a Sparse Autoencoder (8{,}192 features, $8\times$ expansion) on the same residual stream layer targeted by our steering vectors, we find that domain-specific knowledge is encoded in \textit{sparse, localized features} (top-20 differential features per domain), while contrastive vectors activate 50--60\% of all features diffusely. The overlap between domain-specific SAE features and contrastive vector projections is near-zero (0--3/20). This mechanistic finding explains the ``domain style vs.\ domain knowledge'' distinction: contrastive mean-difference vectors shift topic adherence but do not amplify the interpretable features encoding actual domain expertise. We tested this directly: feature-targeted vectors built from SAE decoder columns preserve baseline accuracy better than contrastive vectors (e.g., law at 24\% vs.\ 22\% at $\alpha=3$), but produce no statistically significant improvements on MMLU-Pro. The 0.6B model lacks the domain knowledge to unlock. Feature-targeted steering's advantage is \textit{reduced harm} rather than increased knowledge---relevant for safety-critical deployment.

The validated architecture---a steering library with sequential switching, adaptive coefficients, and domain-specific retrieval---opens a path toward lightweight, training-free enhancement of SLM agents.

All code, data, and steering vectors are available at \url{https://github.com/Shumatsurontek/steering-research}.

% ===================================================================
\section*{Acknowledgments}
Analysis performed using the Transformer Explainer tool~\cite{cho2024transformer} and Neuronpedia interpretability platform. Model weights courtesy of the Qwen team~\cite{qwen3}.

% ===================================================================
\bibliographystyle{plain}
\begin{thebibliography}{99}

\bibitem{turner2023activation}
A.~Turner, L.~Thiergart, D.~Udell, G.~Leech, U.~Mini, and M.~MacDiarmid.
\newblock Activation addition: Steering language models without optimization.
\newblock \textit{arXiv preprint arXiv:2308.10248}, 2023.

\bibitem{gao2025steering}
L.~Gao et al.
\newblock Steering {LLM} reasoning through bias-only adaptation.
\newblock In \textit{EMNLP}, 2025.
\newblock arXiv:2505.18706.

\bibitem{li2025budget}
J.~Li, W.~Zhao, Y.~Zhang, and C.~Gan.
\newblock Steering {LLM} thinking with budget guidance.
\newblock \textit{arXiv preprint arXiv:2506.13752}, 2025.

\bibitem{cunningham2023sparse}
H.~Cunningham, A.~Ewart, L.~Riggs, R.~Huben, and L.~Sharkey.
\newblock Sparse autoencoders find highly interpretable features in language models.
\newblock \textit{arXiv preprint arXiv:2309.08600}, 2023.

\bibitem{lieberum2024gemma}
T.~Lieberum et al.
\newblock Gemma {S}cope: Open sparse autoencoders everywhere all at once on {G}emma 2.
\newblock \textit{arXiv preprint arXiv:2408.05147}, 2024.

\bibitem{wei2022chain}
J.~Wei et al.
\newblock Chain-of-thought prompting elicits reasoning in large language models.
\newblock In \textit{NeurIPS}, 2022.

\bibitem{schick2023toolformer}
T.~Schick et al.
\newblock Toolformer: Language models can teach themselves to use tools.
\newblock In \textit{NeurIPS}, 2023.

\bibitem{li2024inference}
K.~Li, O.~Patel, F.~Vi\'{e}gas, H.~Pfister, and M.~Wattenberg.
\newblock Inference-time intervention: Eliciting truthful answers from a language model.
\newblock In \textit{NeurIPS}, 2024.

\bibitem{zou2023representation}
A.~Zou, L.~Phan, S.~Chen, J.~Campbell, P.~Guo, R.~Ren, A.~Pan, X.~Yin, M.~Mazeika, A.~Dombrowski, S.~Goel, N.~Li, M.~Lin, S.~Wang, C.~Xiao, J.~Zou, Z.~Kolter, and D.~Hendrycks.
\newblock Representation engineering: A top-down approach to {AI} transparency.
\newblock \textit{arXiv preprint arXiv:2310.01405}, 2023.

\bibitem{cho2024transformer}
H.~Cho et al.
\newblock Transformer {E}xplainer: Interactive learning of text generative models.
\newblock 2024.
\newblock \url{https://poloclub.github.io/transformer-explainer/}

\bibitem{qwen3}
Qwen Team.
\newblock Qwen3 technical report.
\newblock \textit{arXiv preprint arXiv:2505.09388}, 2025.

\bibitem{eval-harness}
L.~Gao et al.
\newblock A framework for few-shot language model evaluation.
\newblock \textit{Zenodo}, 2024.
\newblock \url{https://github.com/EleutherAI/lm-evaluation-harness}

\bibitem{wang2024mmlu}
Y.~Wang et al.
\newblock {MMLU-Pro}: A more robust and challenging multi-task language understanding benchmark.
\newblock \textit{arXiv preprint arXiv:2406.01574}, 2024.

\bibitem{bloom2024saelens}
J.~Bloom, S.~Chanin, and contributors.
\newblock {SAELens}: A library for training and analyzing sparse autoencoders.
\newblock 2024.
\newblock \url{https://github.com/jbloomAus/SAELens}

\end{thebibliography}

\end{document}
